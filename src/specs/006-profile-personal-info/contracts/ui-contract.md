# UI Contract: Profile Page

## Input Props
- `userData`: Object containing fields defined in `data-model.md`.
- `onUpdate`: Function to handle field updates.

## Component Interaction
- Editable blocks trigger inline edit mode on click.
- Hover states must apply to container divs of editable blocks.
