import React from 'react';

export default function SearchBar() {
  return (
    <div className="w-full max-w-[896px] mx-auto bg-[#EFF4FF] rounded-2xl border-2 border-[rgba(0,0,0,0.00)] shadow-[0_1px_2px_0_rgba(0,0,0,0.05)] p-4 flex items-center justify-between gap-4">
      {/* Khung chứa Icon và Input */}
      <div className="flex items-center gap-4 flex-grow">
        {/* Icon Kính lúp */}
        <svg
          width="24"
          height="24"
          viewBox="0 0 24 24"
          fill="none"
          xmlns="http://www.w3.org/2000/svg"
          className="w-6 h-6 shrink-0"
        >
          <path
            d="M15.5 14H14.71L14.43 13.73C15.41 12.59 16 11.11 16 9.5C16 5.91 13.09 3 9.5 3C5.91 3 3 5.91 3 9.5C3 13.09 5.91 16 9.5 16C11.11 16 12.59 15.41 13.73 14.43L14.03 14.71V15.5L19 20.49L20.49 19L15.5 14ZM9.5 14C7.01 14 5 11.99 5 9.5C5 7.01 7.01 5 9.5 5C11.99 5 14 7.01 14 9.5C14 11.99 11.99 14 9.5 14Z"
            fill="#091426"
          />
        </svg>
        {/* Ô Nhập liệu */}
        <input 
          type="text" 
          placeholder="Search..." 
          className="w-full bg-transparent border-none outline-none font-inter text-base text-[#091426] placeholder-[#75777D]"
        />
      </div>

      {/* Nút Bộ lọc (Filter) bên phải */}
      <button className="cursor-pointer bg-[#FFF] border border-[#C5C6CD] rounded-xl py-2 px-4 flex items-center gap-2 hover:bg-gray-50 transition-colors shrink-0">
        <svg
          width="18"
          height="12"
          viewBox="0 0 18 12"
          fill="none"
          xmlns="http://www.w3.org/2000/svg"
          className="w-[18px] h-3"
        >
          <path
            d="M7 12H11V10H7V12ZM0 0V2H18V0H0ZM3 7H15V5H3V7Z"
            fill="#091426"
          />
        </svg>
        <span className="text-[#091426] font-inter text-sm font-semibold">
          Filter
        </span>
      </button>
    </div>
  );
}

import React from 'react';

export default function PopularPublishes() {
  // Dữ liệu giả lập từ danh sách sách trong file gốc của bạn
  const books = [
    { id: 1, title: "The Design of Everyday Things", author: "Don Norman", image: "/Rectangle1248.png" },
    { id: 2, title: "The Design of Everyday Things", author: "Don Norman", image: "/Rectangle1248.png" },
    { id: 3, title: "The Design of Everyday Things", author: "Don Norman", image: "/Rectangle1248.png" },
    { id: 4, title: "The Design of Everyday Things", author: "Don Norman", image: "/Rectangle1248.png" },
  ];

  const categories = ["All", "Science", "History", "Arts & Humanities"];

  return (
    <div className="w-full max-w-7xl mx-auto my-12 px-4">
      {/* Tiêu đề mục */}
      <h2 className="text-[#091426] font-inter text-2xl font-bold tracking-[0.01em] mb-6">
        Popular Publishes
      </h2>

      {/* Thanh chọn Danh mục sách */}
      <div className="flex flex-wrap gap-4 border-b border-[#C5C6CD] pb-4 mb-8">
        {categories.map((category, index) => (
          <button
            key={index}
            className={`font-inter text-sm font-semibold px-2 py-1 transition-colors cursor-pointer ${
              index === 0 
                ? "text-[#006F66] border-b-2 border-[#006F66]" 
                : "text-[#75777D] hover:text-[#091426]"
            }`}
          >
            {category}
          </button>
        ))}
      </div>

      {/* Lưới hiển thị danh sách Sách (Grid layout responsive) */}
      <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-6">
        {books.map((book) => (
          <div key={book.id} className="flex flex-col gap-3 group cursor-pointer">
            {/* Ảnh bìa sách */}
            <div className="w-full aspect-[3/4] bg-[#EAEAEA] rounded-lg overflow-hidden shadow-sm group-hover:shadow-md transition-shadow">
              <img
                src={book.image}
                alt={book.title}
                className="w-full h-full object-cover"
                onError={(e) => { e.target.src = 'https://via.placeholder.com/180x240?text=Book+Cover' }}
              />
            </div>
            {/* Thông tin sách */}
            <div className="flex flex-col gap-1">
              <h3 className="text-[#091426] font-inter text-base font-semibold leading-tight line-clamp-2">
                {book.title}
              </h3>
              <p className="text-[#75777D] font-inter text-xs font-medium">
                {book.author}
              </p>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}

import React from 'react';

export default function StudyGroup() {
  // Dữ liệu các nhóm học tập bóc tách từ file gốc
  const groups = [
    {
      id: 1,
      name: "HCMUS - K22 - Artificial Intelligence",
      members: "24 members",
      bgColor: "bg-[#091426]",
      textColor: "text-[#FFF]",
      subColor: "text-[#A1A3A9]"
    },
    {
      id: 2,
      name: "UIT - Computer Science Study Group",
      members: "18 members",
      bgColor: "bg-[#FFF]",
      textColor: "text-[#091426]",
      subColor: "text-[#75777D]",
      border: "border border-[#C5C6CD]"
    }
  ];

  return (
    <div className="w-full max-w-7xl mx-auto my-12 px-4">
      {/* Thanh tiêu đề và nút Xem tất cả */}
      <div className="flex justify-between items-center mb-6">
        <h2 className="text-[#091426] font-inter text-2xl font-bold tracking-[0.01em]">
          Study Group
        </h2>
        <button className="cursor-pointer text-[#006F66] font-inter text-sm font-semibold hover:underline">
          View All
        </button>
      </div>

      {/* Danh sách nhóm học tập dưới dạng Grid */}
      <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
        {groups.map((group) => (
          <div
            key={group.id}
            className={`p-6 rounded-2xl flex flex-col justify-between gap-6 min-h-[160px] shadow-sm hover:shadow-md transition-all cursor-pointer ${group.bgColor} ${group.border || ''}`}
          >
            {/* Tên nhóm và số lượng thành viên */}
            <div className="flex flex-col gap-2">
              <h3 className={`font-inter text-lg font-bold leading-snug ${group.textColor}`}>
                {group.name}
              </h3>
              <p className={`font-inter text-xs font-medium ${group.subColor}`}>
                {group.members}
              </p>
            </div>

            {/* Nút bấm Join Group */}
            <div className="flex justify-end">
              <button className={`font-inter text-sm font-semibold py-2 px-5 rounded-xl transition-opacity hover:opacity-90 ${
                group.id === 1 ? 'bg-[#FFF] text-[#091426]' : 'bg-[#091426] text-[#FFF]'
              }`}>
                Join Group
              </button>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}