# AI Usage Report

    Project: Modern Library Management System 
    Course: CS300 – CSC13002 – Introduction to Software Engineering 
    Group ID: 03
    Group Name: AmeThyst 
    Assignment: PA4-2026

Performed by: All Members | Reviewed by: All Members | Edited by: Vũ Duy Nhất

## Table of Contents

- [AI Usage Report](#ai-usage-report)
  - [Table of Contents](#table-of-contents)
  - [I. 24127028 - Trần Lê Hoàng Gia](#i-24127028---trần-lê-hoàng-gia)
    - [Note 1 - C4 System Context Diagram Conversion to Mermaid Syntax](#note-1---c4-system-context-diagram-conversion-to-mermaid-syntax)
    - [Note 2 - Architectural Model Description for Vision Document](#note-2---architectural-model-description-for-vision-document)
    - [Note 3 - Analytics Guidelines and Chart Library Evaluation](#note-3---analytics-guidelines-and-chart-library-evaluation)
    - [Note 4 - Multi-Level C4 Model Consistency Review \& Format Refinement](#note-4---multi-level-c4-model-consistency-review--format-refinement)
    - [Note 5 - Detection of Generalization Relationship Misuse in Use Case Model](#note-5---detection-of-generalization-relationship-misuse-in-use-case-model)
    - [Note 6 - Summarizing Use Case Artifact Updates into Changes Log](#note-6---summarizing-use-case-artifact-updates-into-changes-log)
  - [II. 24127082 - Phan Lê Anh Minh](#ii-24127082---phan-lê-anh-minh)
    - [Note 1 - Announcement and Study Group Notification Unification](#note-1---announcement-and-study-group-notification-unification)
    - [Note 2 - Admin User Management](#note-2---admin-user-management)
    - [Note 3 - Interactive UI Animations](#note-3---interactive-ui-animations)
  - [III. 24127398 - Nguyễn Nhựt Huy](#iii-24127398---nguyễn-nhựt-huy)
    - [Note 1 - Admin Dashboard \& Authorization Hardening](#note-1---admin-dashboard--authorization-hardening)
    - [Note 2 - Room Check-In \& Check-Out (US1–US4 Core Implementation)](#note-2---room-check-in--check-out-us1us4-core-implementation)
    - [Note 3 - Room Management Dashboard: PIN Time Guard, Calendar Fixes \& Timeslot Beautification](#note-3---room-management-dashboard-pin-time-guard-calendar-fixes--timeslot-beautification)
    - [Note 4 - Auth Session Storage Migration (localStorage → sessionStorage)](#note-4---auth-session-storage-migration-localstorage--sessionstorage)
    - [Note 5 - Authorization Hardening: Branch Lifecycle, Session Invalidation \& Fixes](#note-5---authorization-hardening-branch-lifecycle-session-invalidation--fixes)
  - [IV. 24127408 - Nguyễn Lê Hoàng Khải](#iv-24127408---nguyễn-lê-hoàng-khải)
    - [Note 1 - Refining Study Group invitations and final presentation behavior](#note-1---refining-study-group-invitations-and-final-presentation-behavior)
    - [Note 2 - Adding unit and API integration tests for Create Study Group](#note-2---adding-unit-and-api-integration-tests-for-create-study-group)
    - [Note 3 - Specifying and implementing Admin System Configuration](#note-3---specifying-and-implementing-admin-system-configuration)
  - [V. 24127095 - Vũ Duy Nhất](#v-24127095---vũ-duy-nhất)
    - [Note 1 - Find potential bugs in the system](#note-1---find-potential-bugs-in-the-system)
    - [Note 2 - Drawing Deployment Diagram](#note-2---drawing-deployment-diagram)


## I. 24127028 - Trần Lê Hoàng Gia
### Note 1 - C4 System Context Diagram Conversion to Mermaid Syntax

* **Tool Name, Version, and Platform:** Gemini
* **Access time (Date and Hour):** 2026-08-02 
* **Prompts used:** "Convert this C4 system context diagram into Mermaid.js syntax and refine the notation to conform strictly with standard C4 model conventions."
* **Purpose of use:** Architectural documentation — converting C4 visual models into standardized Markdown diagram code.
* **Content Generated by AI:** Mermaid code block structured using C4Context definitions, including actors, system boundaries, and labeled relationship links.
* **Independent Content & Student Validation:** Checked actor definitions and system boundaries against team specifications, correcting component labels to reflect project scope.

---

### Note 2 - Architectural Model Description for Vision Document

* **Tool Name, Version, and Platform:** Gemini
* **Access time (Date and Hour):** 2026-08-02 
* **Prompts used:** "Write a clear, detailed narrative description for our system architectural model to include in the Vision Document as a reference section."
* **Purpose of use:** Technical documentation — drafting formal architectural overview text for the Vision Document.
* **Content Generated by AI:** Explanatory prose describing the high-level system context, multi-database architecture, and external integration points.
* **Independent Content & Student Validation:** Tailored the generated narrative to explicitly reference our PostgreSQL, Memgraph, and ChromaDB database strategy.

---

### Note 3 - Analytics Guidelines and Chart Library Evaluation

* **Tool Name, Version, and Platform:** Gemini
* **Access time (Date and Hour):** 2026-08-03
* **Prompts used:** "Write implementation guidelines for a library analytics/statistics feature, and compare different web chart libraries (e.g., Chart.js, Recharts, D3) and chart types for displaying library metrics."
* **Purpose of use:** Technical evaluation — selecting data visualization libraries and planning analytics metrics.
* **Content Generated by AI:** Technical comparison matrix evaluating frontend rendering libraries alongside guidance on matching chart types to metric dimensions.
* **Independent Content & Student Validation:** Selected the chart setup best suited to our React frontend stack and integrated the analytics guidelines into feature planning.

---

### Note 4 - Multi-Level C4 Model Consistency Review & Format Refinement

* **Tool Name, Version, and Platform:** Gemini
* **Access time (Date and Hour):** 2026-08-05
* **Prompts used:** "Check the consistency of my C4 architectural model across three levels (Context, Container, Component), flag any structural discrepancies, and refine the Mermaid diagram formatting."
* **Purpose of use:** Architecture validation — ensuring logical alignment across C4 abstraction layers.
* **Content Generated by AI:** Consistency review report highlighting missing boundary details, along with updated and unified Mermaid diagram code across all three levels.
* **Independent Content & Student Validation:** Reviewed cross-level interface mappings against actual backend endpoints and updated container names to match project services.

---

### Note 5 - Detection of Generalization Relationship Misuse in Use Case Model

* **Tool Name, Version, and Platform:** Gemini
* **Access time (Date and Hour):** 2026-07-08 
* **Prompts used:** "Analyze my Use Case model and detect any misuse of generalization relationships between actors or use cases, explaining where standard associations, `<<include>>`, or `<<extend>>` relationships should be used instead."
* **Purpose of use:** System modeling validation — identifying and correcting improper UML generalization relationships within the Use Case diagram.
* **Content Generated by AI:** A structural review highlighting incorrectly applied generalization arrows (e.g., confusing functional delegation with inheritance) and providing recommended UML updates.
* **Independent Content & Student Validation:** Evaluated the flagged relationships against standard UML specifications, updated the Mermaid Use Case diagram to use proper relationship types, and synchronized the changes in the project documentation.

---

### Note 6 - Summarizing Use Case Artifact Updates into Changes Log

* **Tool Name, Version, and Platform:** Gemini
* **Access time (Date and Hour):** 2026-08-08 
* **Prompts used:** "Summarize the recent updates made to our Usecase-Diagram and Usecase-Specification documents, and compile them into a structured Changes.md file with provided format."
* **Purpose of use:** Documentation & Version Control — aggregating structural revisions across system use case diagrams and specifications into a centralized changelog (`changes.md`).
* **Content Generated by AI:** A concise, bulleted Markdown changelog detailing modified actors, updated main/alternative execution flows, and newly added specification attributes.
* **Independent Content & Student Validation:** Cross-checked the generated revision summary against recent Git commits and diagram updates to ensure all noted changes accurately reflected the actual modifications made to the project artifacts.

## II. 24127082 - Phan Lê Anh Minh
### Note 1 - Announcement and Study Group Notification Unification

* **Tool Name, Version, and Platform:** Claude Code in SpecKit
* **Access time (Date and Hour):** 2026-08-02
* **Prompts used:**
  * `/speckit.specify` — "Specify feature `029-announcement-notification-unification`. The feature must: (1) reject announcement expiry dates earlier than today at the date picker, client validation, and backend validation while allowing today, future dates, and no expiry date; (2) restore the unread indicator when an existing announcement changes from a non-active status to `active`, while preserving its `announce_id` and `created_at`; and (3) combine librarian announcements, Study Group invitations, and Study Group lifecycle notifications under one notification bell. Do not add database columns, change `created_at`, or implement cross-device read-state synchronization. Preserve existing Study Group actions and role-specific behavior."
  * `/speckit.plan` — "Analyze the existing announcement API, status-update service, Socket.IO events, notification bell, localStorage read state, and Study Group notification flow. Produce a plan for Spec 029 covering date-only expiry validation, republish transition detection, user-scoped seen-announcement storage, a unified notification view model, one bell owner, and regression checks. Reuse existing APIs and schema. List the exact files expected to change and identify duplicate UI or listener logic that should be removed."
  * `/speckit.tasks` — "Generate dependency-ordered tasks for Spec 029 using the `[ID] [P?] [Story]` format. Include baseline flow tracing; frontend and backend expiry validation; non-active-to-active republish detection; preservation of UUID and `created_at`; Socket.IO handling; user-scoped and idempotent seen-ID updates; normalization and timestamp sorting of announcement and Study Group items; aggregate unread calculation; removal of the duplicate bell; preservation of invitation actions and lifecycle navigation; and final checks for schema changes, duplicate listeners, unrelated files, and mock data."
  * `/speckit.implement` — "Implement the approved tasks in `src/specs/029-announcement-notification-unification/tasks.md`. Keep the implementation within the documented file-impact list. Add date-only expiry validation, detect a successful non-active-to-active announcement transition, preserve the existing announcement row and timestamps, and make a republished announcement unread again for the current user. Consolidate all supported notification types under the existing canonical bell and retain current Study Group actions. Do not add migrations, `published_at`, packages, mock data, or new Study Group backend behavior. Update task status only when supported by the implementation or verification result."
* **Purpose of use:** To define and implement a single notification experience for librarian announcements and Study Group events, prevent invalid expiry dates, and make republished announcements appear unread again without changing the database schema.
* **Content Generated by AI:**
  * SpecKit documentation under `src/specs/029-announcement-notification-unification/` (specification, plan, tasks, supporting research, data model, quickstart, and contracts).
  * Client-side expiry-date restrictions and validation in the announcement form/manager flow.
  * Backend date validation and announcement status-transition handling.
  * User-scoped announcement seen-state and republish unread handling.
  * Normalization of announcement and Study Group items for a shared dropdown and unread indicator.
  * Refactoring of the notification bell/dropdown integration to remove the duplicate bell owner.
* **Independent Content & Student Validation:** The student narrowed the generated solution to the existing database model and explicitly rejected proposals that introduced `published_at`, changed `created_at`, or required cross-device synchronization. The student reviewed the combined notification flow, removed unnecessary generated UI files where existing components could be reused, checked that role-specific Study Group requests remained isolated, and inspected the final diff for duplicate bell logic and unrelated changes. Verification evidence should be read from the completed items in `tasks.md`, the manual scenarios in `quickstart.md`, and the final committed diff.

---

### Note 2 - Admin User Management

* **Tool Name, Version, and Platform:** Claude Code in SpecKit
* **Access time (Date and Hour):** 2026-07-31
* **Prompts used:**
  * `/speckit.specify` — "Specify feature `030-admin-user-management` for an administrator-only user management dashboard. Adapt the supplied prototype to the existing Next.js Atomic Design structure and connect it to PostgreSQL-backed APIs instead of runtime mock data. Cover KPI statistics, paginated users, debounced search, role and status filters, user details, role changes, suspension with a required reason, restoration, filtered UTF-8 CSV export, and administrative audit records. Include admin authorization, suspended-account rejection, self-mutation prevention, last-active-admin protection, parameterized queries, CSV formula-injection protection, localization, dark mode, accessibility, and responsive behavior."
  * `/speckit.plan` — "Analyze the existing admin layout, navigation, authentication middleware, user model, API utilities, localization, and supplied UI prototype. Plan the database, service, controller, route, and responsive frontend changes for Spec 030. Define the API contracts for statistics, list, details, role change, suspend, restore, export, and audit records. Include transaction boundaries and authentication checks for password login, Google OAuth, and authenticated API requests. List the expected files and verification scenarios without changing unrelated configuration."
  * `/speckit.tasks` — "Generate dependency-ordered tasks for Spec 030. Organize them into baseline analysis; database and audit support; authentication and authorization; read APIs; mutation APIs; CSV export; responsive UI integration; details and management dialogs; localization and accessibility; and final security and scope review. Each task must identify its target file, expected behavior, relevant security invariant, and completion condition."
  * `/speckit.implement` — "Implement Spec 030 using the existing AmeThyst Library architecture. Add admin-only services, controllers, and routes under `/api/admin`; implement safe filtering, pagination, KPI statistics, details, role/status changes, audit writes, and full filtered CSV export. Reject suspended accounts in local login, Google OAuth, and authenticated API middleware. Build the responsive User Management interface from reusable KPI, filter, table, pagination, details-modal, and management-modal components. Use real API data, refresh relevant data after mutations, localize English and Vietnamese text, support dark mode and accessibility, and do not modify unrelated test configuration or leave runtime mock data."
* **Purpose of use:** To convert the supplied Admin User Management prototype into a secure, responsive, localized feature backed by real application data and protected administrative APIs.
* **Content Generated by AI:**
  * SpecKit documentation under `src/specs/030-admin-user-management/` (specification, plan, tasks, supporting documents, and API contract).
  * Admin services, controllers, and routes for statistics, user listing/details, mutations, audit records, and CSV export.
  * Authentication and authorization changes related to suspended accounts and admin-only access.
  * The admin dashboard's API state, filtering, pagination, mutation refresh, feedback states, and export flow.
* **Independent Content & Student Validation:** The student compared the generated interface with the supplied design, removed runtime mock data, and reviewed the API integration and component decomposition. Security-sensitive behavior—including self-mutation prevention, last-active-admin protection, suspended-account handling, route authorization, parameterized queries, and CSV formula-injection protection—was checked against the specification and final diff. The student also reviewed localization, null-date handling, responsive layouts, modal behavior, and cleanup of the temporary CSV download URL.

---

### Note 3 - Interactive UI Animations

* **Tool Name, Version, and Platform:** Claude Code in SpecKit
* **Access time (Date and Hour):** 2026-08-02
* **Prompts used:**
  * `/speckit.specify` — "Specify a reusable interactive animation feature for the AmeThyst Library client. Keep the landing page as one hero by placing a cursor-controlled bookshelf spotlight behind the existing `AmeThyst Library` title, subtitle, and actions. Add cursor-reactive particles to the library hero and subtle sparkle effects to authentication visuals. Preserve text readability, pointer interaction, responsive behavior, touch-device fallback, `prefers-reduced-motion`, resource cleanup, and identical server/client initial markup to avoid hydration mismatch."
  * `/speckit.plan` — "Analyze the landing page, library `HeroSection`, login brand panel, and registration template. Plan a small reusable effects layer containing a canvas particle field, authentication sparkle overlay, and spotlight-reveal hero. Keep browser capability checks and random initialization inside post-mount effects, define responsive density and spotlight sizing, and include cleanup for animation frames, listeners, and resize observers. List only files required by the feature."
  * `/speckit.tasks` — "Generate tasks to add the visual asset; create and export the reusable particle, sparkle, and spotlight components; integrate them into the existing landing, library, login, and registration sections; preserve translated content and navigation; implement responsive, touch, and reduced-motion behavior; remove hydration-sensitive initial render logic; verify stacking and pointer events; and run the applicable client checks."
  * `/speckit.implement` — "Implement the approved animation tasks without changing business logic. Build reusable effects with container-relative pointer tracking, device-pixel-ratio canvas scaling, responsive density, visibility pausing, reduced-motion handling, resize observation, and complete cleanup. Keep the bookshelf spotlight behind the existing landing content, darken the area outside the spotlight, and avoid creating a second hero. Do not make initial JSX or class names depend on browser-only APIs, time, or random values. Integrate the effects through existing component exports and preserve all existing links and actions."
* **Purpose of use:** To introduce a cohesive set of interactive visual effects across the landing, authentication, and library hero sections while maintaining accessibility, performance, responsiveness, and hydration safety.
* **Content Generated by AI:**
  * Reusable interactive particle, sparkle-overlay, and spotlight-reveal components under the client component structure.
  * Effect component exports and integration into the landing page, library hero, login panel, and registration template.
  * Responsive pointer tracking, canvas scaling, reduced-motion behavior, touch fallback, and resource-cleanup logic.
  * Adjustments that keep the bookshelf spotlight as the background of the existing landing hero.
  * Refactoring of browser-dependent initialization to prevent server/client hydration differences.
* **Independent Content & Student Validation:** The student iterated on the visual result by repositioning the spotlight effect behind the original hero content, enlarging the spotlight radius, darkening the surrounding area, and adjusting particle color and density. After a hydration warning was observed, the student reviewed the generated component behavior and directed the fix so browser-only checks and random initialization occurred after mount while the first server and client renders remained identical. The student also reviewed foreground contrast, button interaction, responsive behavior, reduced-motion fallback, and effect cleanup.

## III. 24127398 - Nguyễn Nhựt Huy
### Note 1 - Admin Dashboard & Authorization Hardening

- **Tool Name, Version, and Platform:** deepseek-v4-flash-free, via opencode CLI
- **Access Time (Date and Hour):** 10:53 ICT, July 31, 2026
- **Prompts used:**
  - Build the admin dashboard, reusing the existing user/librarian dashboard shell (NavBar, Footer, sidebar pattern)
  - Add admin tabs: User Management, Roles & Permissions, Statistics, System Configuration
  - Decide how to restrict admin access to public pages and block them from `'/'`, `'/library*'`, `'/study-together*'`, `'/map*'`
  - Audit and fix authorization so normal/unauthenticated users cannot call librarian/admin APIs
- **Purpose of Use:** Built the admin dashboard with a role-aware shell and sidebar, redirected admins away from public pages, and hardened backend authorization so librarian/admin routes reject unprivileged callers
- **Content Generated by AI:**
  - **Frontend — Admin Shell (1 file):**
    - `app/dashboard/admin/layout.tsx` — admin layout mirroring user/librarian layouts with `NavBar variant="admin"`, `AdminDashboardSidebar`, main, and Footer
  - **Frontend — Components (3 files):**
    - `app/components/organisms/AdminDashboardSidebar.tsx` (new) — 4 tabs with icons and active-state highlight
    - `app/components/atoms/PublicPageAdminGuard.tsx` (new) — redirects admins from `'/'`, `'/library'`, `'/study-together'`, `'/map'` to `/dashboard/admin`
    - `NavBar.tsx` — added `variant = 'default' | 'admin'`; minimal navbar (logo, notification bell, settings, avatar) for admins; LIMA logo links to admin dashboard for admins
  - **Frontend — Layouts & Pages (6 files):**
    - `app/dashboard/layout.tsx` — role guard extended to allow `admin`; path-based section access (admin section admin-only, librarian section librarian/admin)
    - `app/layout.js` — mounts `PublicPageAdminGuard`
    - `app/dashboard/admin/page.tsx`, `authorization/page.tsx`, `statistics/page.tsx`, `system/page.tsx` — placeholder pages using i18n keys + "Coming Soon"
  - **Locales (2 files):** Added `admin.*` keys to `en.json` and `vi.json` (EN: User Management, Roles & Permissions, Statistics, System Configuration; VI: Quản lý người dùng, Vai trò & Quyền hạn, Thống kê, Cấu hình hệ thống); sidebar keys renamed to `admin.sidebar_*`
  - **Backend — Routes (2 files):**
    - `server/src/routes/library.routes.mjs` — protected book CRUD (`POST /api/books/upload-cover`, `POST /api/books`, `PUT /api/books/:id`, `DELETE /api/books/:id`) with `verifyToken, authorizeRole('librarian','admin')`
    - `server/src/routes/dashboard.librarian.routes.mjs` — protected operational routes (`GET /pickups`, `POST /verify-pin`, `POST /confirm-borrowing`, `POST /cancel-borrowing`) with `authorizeRole('librarian')`
  - **Backend — Middleware (2 files, reviewed):** `verifyToken` re-reads role from DB per request; `authorizeRole(...allowedRoles)` returns 403 on mismatch
- **Independent Content & Student Validation:** Student reviewed the admin shell against the existing dashboard pattern, confirmed the admin public-page redirect decision (admins blocked from public pages), verified admin tabs and sidebar keys render correctly in both locales, and validated the authorization changes with eslint, `tsc --noEmit`, `next build`, `node --check`, and `npm run sync-locales`

---

### Note 2 - Room Check-In & Check-Out (US1–US4 Core Implementation)

- **Tool Name, Version, and Platform:** deepseek-v4-flash-free, via opencode CLI
- **Access Time (Date and Hour):** 23:00 – 23:40 ICT, July 31, 2026
- **Prompts used:**
  - Speckit `/speckit.implement` execution of `specs/029-room-checkin-checkout/tasks.md` (T001–T037), including the user-added per-user room reservation limit (`MAX_ROOM_RESERVE_LIMIT = 5`)
  - *"don't create test file because it is not necessary, please implement the core feature"* → test-writing tasks (T014/T021/T028) intentionally skipped; the pre-existing `room.services.spec.mjs` was still run and passes
- **Purpose of Use:** Implemented the core Room Check-In & Check-Out feature end-to-end across all four user stories: (US1) user generates a check-in PIN, (US2) librarian verifies PIN and confirms check-in, (US3) user confirms check-out after the slot elapses (with scheduler backfill), (US4) date-filtered reservation history
- **Content Generated by AI:**
  - **US1 — PIN generation (T008–T013 ✓):**
    - `room.services.mjs` — `generateRoomPin`/`cleanupRoomPin` wrapping the Phase 2 shared `generateEntityPin` core (table `reserve_room`, id `reserve_id`, allowed `['reserved','pending']`, reset `reserved`, pending `pending`, `RESERVATION_NOT_FOUND` 404, `PIN_GENERATION_FAILED` 500, 3-min expiry); kept transactional `createReservation`/`cancelReservation` with the limit guard
    - `room.models.mjs` — `cleanupRoomPin` (clears pin/expired_at, reverts `pending → reserved`); `findUserReservations` now also returns `pin`/`expiresAt`
    - `room.controllers.mjs` + `room.routes.mjs` — `generateRoomPinController`, `cleanupRoomPinController`; `POST /api/rooms/reserve/:reserveId/pin` and `POST /api/rooms/reserve/:reserveId/pin/cleanup` (both `verifyToken`)
    - `ReservationCard.tsx` — Create/View PIN button calling the endpoint via `apiFetch`, opening `PinModal` with `useCountdownFromDate`; idempotent PIN surface on reopen (local `pin`/`expiresAt` state, since mutating props violates the React Compiler purity rule)
  - **US2 — Librarian check-in (T015–T020 ✓):**
    - `room.models.mjs` — `findPendingRoomReservationByPin` (pin + `status='pending'` + `expired_at > NOW()`, joins users/room_avail/study_room/branches), `confirmRoomCheckin` (sets `used`, `checkin_time`, clears pin/expired_at, status guard)
    - `dashboard.librarian.services.mjs` — `verifyRoomPin` (returns reservation/user/room details; `PIN_NOT_FOUND` 404), `confirmRoomCheckin` (transactional via `pool.connect()` + BEGIN/COMMIT/ROLLBACK)
    - `dashboard.librarian.controllers.mjs` + `routes` — `verifyRoomPinController`, `confirmRoomCheckinController`; `POST /dashboard/librarian/verify-room-pin` and `/confirm-room-checkin` (both `verifyToken` + `authorizeRole('librarian')`)
    - `RoomCheckinTab.tsx` (new organism) — 3-step flow (6-digit OTP input → user/room/reservation details → confirm modal), reusing the `InlinePinVerification` step pattern; registered in `LibrarianBookDashboard.tsx` and `SubTabBar.tsx` via a new `room_checkin` tab (`librarian.subtab_room_checkin`)
  - **US3 — Check-out (T022–T027 ✓):**
    - `room.models.mjs` — `findReturnRecord`, `checkoutRoom` (idempotent return_room insert + `GREATEST(reserve_num - 1, 0)` decrement), `backfillDefaultedCheckouts` (used reservations with `checkin_time`, slot `start_date + end_time < NOW()`, `WHERE NOT EXISTS` in return_room; checkout_time = slot end_time), `findReservationOwnedBy`
    - `room.services.mjs` — `confirmCheckout` (transactional; validates ownership, `status='used'`, slot elapsed via local-date formatting to avoid UTC shift; errors `RESERVATION_NOT_FOUND` 404 / `CHECKOUT_NOT_ELIGIBLE` 409; returns existing record if already returned)
    - `pinScheduler.mjs` — `backfillDefaultedCheckouts` wired into both `runStartupPinCleanup` and `startPeriodicPinCleanup`
    - `room.controllers.mjs` + `routes` — `confirmCheckoutController`; `POST /api/rooms/reserve/:reserveId/checkout`
    - `ReservationCard.tsx` — "Checkout Confirm" button replaces Create PIN/Cancel when `status === 'used'` and the slot has elapsed (ticking `now` state updated every 30s to keep the render pure); confirmation modal; refresh via page callback
  - **US4 — History (T029–T033 ✓):**
    - `room.models.mjs` — `findRoomHistory(userId, from, to)` with optional inclusive date-range filters on `start_date`, join `return_room`, exposes `checkinTime`/`checkoutTime`, ordered `start_date DESC`
    - `room.services.mjs` — `getRoomHistory` validating `YYYY-MM-DD` params
    - `room.controllers.mjs` + `routes` — `getRoomHistoryController`; `GET /api/rooms/history` (`verifyToken`)
    - `PastBookingsTable.tsx` — added From/To date filter controls (Apply/Clear) and Check-in/Check-out columns; `dashboard/user/reservations/page.tsx` fetches `GET /api/rooms/history?from=..&to=..` on filter
  - **Locales (2 files):** added `room.view_pin` ("View PIN"/"Xem mã PIN"); moved the `room_checkin_*` keys from the wrong `verification` namespace into `librarian.*` in both en.json and vi.json (they were added under `verification` during an earlier phase, breaking `librarian.room_checkin_*` lookups)
- **Independent Content & Student Validation:** Validated the core feature with `npm test` (13 files / 96 tests pass, including the 11 room service tests), `npx tsc --noEmit` (clean), `npx eslint` on all changed files (only the pre-existing `<img>` warning and an unchanged pre-existing `useEffect` pattern), JSON validation + a key-resolution script confirming every new i18n key exists in both en.json and vi.json, and `node scripts/sync-locales.mjs` (files in sync). T037 (manual E2E against the running app) was left unmarked — the scenarios in `quickstart.md` should be executed when the services are running. Test-writing tasks T014/T021/T028 were skipped per the user's explicit instruction. The pre-implementation and post-implementation git commit hooks (`/speckit.git.commit`) remain optional and were not executed.

---

### Note 3 - Room Management Dashboard: PIN Time Guard, Calendar Fixes & Timeslot Beautification

- **Tool Name, Version, and Platform:** deepseek-v4-flash-free, via opencode CLI
- **Access Time (Date and Hour):** 20:30 – 21:00 ICT, August 1, 2026
- **Prompts used:**
  - Implemented `specs/030-room-management-dashboard` calendar work: made the timeline window DB-driven (from real `room_avail` slots), made every bookable room always render (even with no events), and fixed the events-never-visible date-key bug
  - *"fix the issue when the PIN popup shows 409 PIN_NOT_ELIGIBLE..."* → added slot-start time guard to `generateRoomPin`
  - Seeded the branch-1 `reserve_room` table with realistic demo reservations across 2026-08-01..08-07 so the calendar and overview KPIs show real data
  - *"the occupied KPI shows 3 rooms occupied at 21:00 but nothing is booked then"* → timezone fix in `VIETNAM_TIME_SQL`
  - *"make the PIN error message fade out automatically after 3 seconds"* → added auto-fade to `ReservationCard`
  - *"add the avatar of the user into the reservation detail modal"* → avatar in `findReservationDetail`
  - *"beautify the timeslots in the calendar — they look too plain, I want nicer gradient cards and a 'now' line"* → polished day/week event blocks with `statusBlockStyles` and a live red "now" indicator
- **Purpose of Use:** Completed the Room Management Dashboard calendar: corrected the check-in PIN eligibility logic, fixed the occupied KPI timezone comparison, seeded demo reservations, fixed the event date-key mismatch that hid all events, and redesigned the day/week timeslot UI for a polished look
- **Content Generated by AI:**
  - **PIN time guard (root cause of the 409 popup):** `room.services.mjs` `generateRoomPin` now calls `roomModel.findReservationSlotStart` and returns `404 RESERVATION_NOT_FOUND` (not owned/invalid status) or `409 PIN_NOT_ELIGIBLE_YET` when the slot has not started (checked in-DB against Vietnam time via `VIETNAM_NOW_SQL`), else falls through to `generateEntityPin`; tests updated with the new mock chain
  - **Seeded data:** 34 INSERTs into branch-1 `reserve_room` (2026-08-01..08-07: today 13, then 5/5/3/3/3/3) with `used`/`reserved`/`pending` statuses, slot start/end times from `room_avail`, and avatars for 10 demo users
  - **occupied KPI fix:** `room.models.mjs` `VIETNAM_TIME_SQL` changed to `((CURRENT_TIME AT TIME ZONE 'Asia/Ho_Chi_Minh')::time)` so the timetz value is compared in VN local time instead of being coerced to UTC (wrong windows previously matched, showing 3 "occupied")
  - **Avatar in detail modal:** `findReservationDetail` SELECT + mapper now include `u.avatar`; `RoomManagementDashboard.tsx` detail modal renders a rounded avatar with initials fallback
  - **PIN error auto-fade:** `ReservationCard.tsx` routes all `setPinError` calls through `showPinError`, fading the message after 3s and clearing state at 3.3s
  - **DB-driven calendar window:** removed the hard-coded `WINDOW_START/END/HOUR_LABELS/HOUR_GRID/WEEK_CELL_HEIGHT`; backend `findRoomSchedule` now queries all bookable branch rooms (EXISTS `room_avail`) and returns `timeWindow {start,end}` = MIN..MAX slot times; client derives `windowStart/windowMinutes/gridSteps/stepMin/weekCellHeight` from it (fallback 07:00–23:00); branch 2 now also lists its 3 rooms (0 events)
  - **Events-never-visible bug fix (root cause):** schedule API returned `rr.start_date` as an ISO timestamp (`2026-07-31T17:00:00.000Z`), so the day-cell key `roomId|YYYY-MM-DD` never matched → changed to `TO_CHAR(rr.start_date, 'YYYY-MM-DD') AS "date"`; events now render
  - **Timeslot beautification:** new `statusBlockStyles(status)` returns gradient card classes + accent bar color per status (reserved = navy blue, used = green, pending = amber); day view (horizontal timeline) and week view (vertical cells) event buttons are now rounded gradient cards with a left accent bar, title + time chip, soft banded hour strips, subtle gridlines, and a red "now" dot/line positioned by Vietnam time (`nowLineLeft` from `vietnamNowMin`)
- **Independent Content & Student Validation:** Validated with live API checks (branch 1 `timeWindow 07:30–17:30`, 3 rooms, 17–18 events; `GET /api/rooms/schedule` returns correct `date=2026-08-01`), full server test suite (15 files / 127 tests pass), `npx tsc --noEmit` (clean), `npx eslint` on changed files (only pre-existing `<img>` warnings), and a production `npm run build` that succeeds. Student directed the fix boundaries (time guard, seed scope, occupied KPI), confirmed the events were previously invisible due to the date-key mismatch, and reviewed the beautified day/week views

---

### Note 4 - Auth Session Storage Migration (localStorage → sessionStorage)

- **Tool Name, Version, and Platform:** deepseek-v4-flash-free, via opencode CLI
- **Access Time (Date and Hour):** 14:30 ICT, August 6, 2026
- **Prompts used:**
  - *"move the token from localStorage to sessionStorage (dies on browser close — swap getItem/setItem in the files above), at the cost of re-login per browser restart."*
  - Follow-up (authorization research): *"scan through my sql file and tell me if a role switch can simply done by modifying the role column in user table"* → confirmed via `database/init_db/postgres/04_datauser.sql` that a plain `UPDATE users SET role = ...` works (single `role` column, CHECK `chk_role` limited to `admin`/`librarian`/`user`), with caveats: role changes take effect on next login (JWT embeds role at login), and promoted librarians/admins should have a `branch_id` (nullable FK)
- **Purpose of Use:** Migrated all client-side auth session storage (`token`, `user`) from `localStorage` to `sessionStorage` so sessions expire on browser close, requiring re-login per browser restart; also audited the users table to confirm whether an admin authorization page can be implemented by simply updating the `role` column
- **Content Generated by AI:**
  - **Auth write sites (5 files):** `app/login/page.tsx`, `app/auth/callback/page.tsx`, `app/verify-email/page.tsx`, `app/utils/user.ts` (get/isLoggedIn/getAuthToken/logout/updateStoredUser), `app/utils/apiClient.ts` — all `token`/`user` `getItem`/`setItem`/`removeItem` calls swapped from `localStorage` to `sessionStorage`
  - **Auth read sites (14 files):** `app/library/[id]/page.tsx`, `app/dashboard/user/page.tsx`, `app/dashboard/user/recommendations/page.tsx`, `app/components/ui/ImageUploader.jsx`, `app/components/molecules/BookCard.tsx`, `app/components/molecules/BorrowedBookCard.tsx`, `app/components/organisms/PopularPublishes.tsx`, `app/components/organisms/BookPickupTab.tsx`, `app/components/organisms/BookManagementTab.tsx`, `app/components/organisms/BookReturnTab.tsx`, `app/components/modals/StockTransferModal.jsx`, `app/components/modals/BookFormModal.jsx`, `app/components/modals/BookEditModal.jsx`, `app/components/modals/BookDeleteModal.jsx` — every `localStorage.getItem('token')` swapped to `sessionStorage.getItem('token')` so auth stays consistent within a session
  - **Intentionally untouched (non-auth localStorage):** `ThemeProvider.tsx` (`theme`), `I18nProvider.tsx` (`locale`), `useAnnouncementBell.ts` (`amethyst:announcements:*`), `AuthActions.tsx` (invitation/notification read keys) — these remain persistent across browser restarts by design
- **Independent Content & Student Validation:** Verified via grep that zero `localStorage` reads/writes of the `token`/`user` keys remain in the codebase (all auth storage now `sessionStorage`). `npx tsc --noEmit` was not run because TypeScript is not installed as a project dependency; the change is a plain `Storage` API identifier swap with no type impact. Student specified the exact migration intent (re-login per browser restart is an accepted trade-off) and the authorization-page SQL review scope

---

### Note 5 - Authorization Hardening: Branch Lifecycle, Session Invalidation & Fixes

- **Tool Name, Version, and Platform:** deepseek-v4-flash-free, via opencode CLI
- **Access Time (Date and Hour):** 11:30 – 13:00 ICT, August 7, 2026
- **Prompts used:**
  - *"column u.admin_since does not exist"* → diagnosed the running Postgres volume never applied `09_admin_guard_migration.sql` (docker-entrypoint-initdb.d runs only on first init) and applied it to the live `library_postgres` container
  - *"The invitation email could not be sent. No account was created."* → root cause was the server process running with a stale `MAIL_PASS`; current `server/.env` credentials verified working via a direct nodemailer send
  - *"Branch ID Lifecycle for Librarian Roles: when promoting to Librarian, require/allow setting a branch_id; when demoting away from Librarian, set branch_id to NULL"* → implemented branch assignment on role transitions
  - *"Demoting an admin throws FOR UPDATE is not allowed with aggregate functions"* and *"Promoting a user to Librarian does not invalidate existing JWT tokens"* → two bug fixes
  - *"must_change_password flag not updated after first-time password reset"* → frontend cache sync fix
  - *"make the target and change columns on history align perfectly vertical"* and *"refactor the layout, the change column length can push the target column"* → history table alignment
- **Purpose of Use:** Hardened the authorization module: applied the missing `admin_since` migration to the running database, diagnosed the admin-invite email failure, implemented the librarian `branch_id` lifecycle (required on promote-to-librarian, cleared on any transition away), fixed the `FOR UPDATE` + aggregate SQL error on demotion, invalidated JWTs on promote (not just demote), and fixed the stale `must_change_password` cache so invitees are unblocked after their first password change
- **Content Generated by AI:**
  - **Database migration applied (runtime):** `09_admin_guard_migration.sql` executed against `library_db` — added `users.admin_since` and backfilled both existing admins from `created_at`
  - **Email invite diagnosis:** confirmed `sendAdminInviteEmail` works with the current `server/.env`; failure was a stale env in the long-running server process, not a code bug
  - **Branch lifecycle (backend):** `server/src/services/authorization.services.mjs` — `assertValidBranch` (`BRANCH_REQUIRED`/`BRANCH_NOT_FOUND`); `promoteUserService`/`demoteUserService` accept `branchId` and set `branch_id = $3` (`branchId` when target role is librarian, `NULL` otherwise); `authorization.controllers.mjs` forwards `branchId`
  - **Branch lifecycle (frontend):** `RoleChangeModal.tsx` fetches `/api/branches` and renders a `CustomSelect` when the target role is librarian (loading/unavailable states, submit blocked without a selection); `authorizationApi.ts` adds `getBranches()` + `branchId` args to promote/demote; `RoleManagementPanel.tsx` passes it through; new `branch_*` i18n keys in en/vi
  - **Demotion `FOR UPDATE` fix:** `getActiveAdminCountForUpdate` now locks rows in a subquery (`FROM (SELECT user_id ... FOR UPDATE) locked`) before `COUNT(*)`, since `FOR UPDATE` is invalid on an aggregate
  - **JWT invalidation on promote:** promote `UPDATE` now also increments `token_version` (matching demote) so old tokens are rejected by `auth.middleware.mjs:27`; promote response message now states sessions were terminated
  - **`must_change_password` cache fix:** `SecurityFormCard.tsx` calls `updateStoredUser({ must_change_password: false })` after a successful password change so `useStoredUser()`-based route guards stop redirecting to `/profile/security`
  - **History table alignment:** removed the row-only `gap-4` (the root cause of header/row column drift) and gave the change column a fixed `w-[240px]` in both header (`AuthorizationHistoryPanel.tsx`) and rows (`HistoryLogRow.tsx`), with the change badge `truncate` and action/NEW badges `shrink-0` so long change text can no longer push the target column
- **Independent Content & Student Validation:** Verified with `node --check` on changed server files and `npx tsc --noEmit` on the client (both clean); validated the branch lifecycle end-to-end against the live DB with a temporary user (missing branch → `BRANCH_REQUIRED`, invalid → `BRANCH_NOT_FOUND`, promote set branch 2, demote cleared it) and cleaned up the test row afterwards. Student reviewed the `admin_since` migration, confirmed the branch lifecycle requirement, and verified the history layout visually. The FOR UPDATE subquery fix and promote token_version increment were confirmed by reading the generated SQL against the `users` schema

## IV. 24127408 - Nguyễn Lê Hoàng Khải
### Note 1 - Refining Study Group invitations and final presentation behavior

- **Tool name, version, and platform:** OpenAI Codex, GPT-5.6 Sol, OpenAI Codex coding-agent workspace connected to the local project repository.
- **Access Time (Date and Hour):** July 31, 2026 (UTC+7).
- **Prompts used:**
  - “Trong trang Study Together, bỏ filter subjects đi và kéo dài Search groups ra để bù đắp khoảng trống. Bên Dashboard/Your Study Groups không lưu tag Cancelled nữa, đồng thời bỏ filter cho tag Cancelled. Chỉnh sửa tài liệu đặc tả liên quan trong `specs/026-study-group-feature` hoặc `specs/027-study-group-logic-hardening` nếu cần.”
  - “Nên phân biệt giữa invite và request trong trang Your Study Groups bằng một tab invitation hoặc render card khác nhau. Invite từ creator không thể có Cancel Request; người nhận chỉ có thể approve, deny hoặc để lời mời expire.”
  - “Làm theo plan nhưng chưa đụng gì đến unit tests.”
  - “Trong chức năng invitation của Study Group, thêm constraint không được mời tài khoản có role librarian hoặc admin; đồng thời không hiển thị Join Group cho librarian trong Study Together.”
  - “Nếu B đang có lời mời từ A thì Study Together không được hiển thị như một Pending request. Nút hành động phải là Accept Invitation và khi B bấm thì xem như đồng ý lời mời.”
  - “Trong Dashboard/Your Study Groups/Invitations chỉ hiển thị hai dòng card và sử dụng nút đổi trang như Groups I Created và Groups I Joined.”
  - “Xử lý overflow cho title và description khi gặp chuỗi quá dài; title ngoài Study Group card dùng dấu ba chấm và không xuống dòng, description tối đa hai dòng và chuỗi không có khoảng trắng không được tràn khỏi card.”
  - “Trong mọi popup của Study Card, Edit Settings và nút X phải đồng hàng với tags và ở vị trí cố định, không phụ thuộc độ dài title. Title và tag trong popup phải hiển thị đầy đủ, tự xuống dòng và không dùng dấu ba chấm như card bên ngoài.”
- **Purpose of use:** Apply the final team-review corrections to invitation semantics, role eligibility, dashboard organization, card text containment, and detail-popup layout while preserving the project’s existing Study Group visual language.
- **Content generated by AI:** A dedicated Invitations tab with invitation-specific Accept/Deny cards and pagination; relationship-type-aware Study Together actions; direct invitation acceptance from discovery; backend rejection of librarian/admin invitees; staff Join Group suppression; Cancelled-list/filter removal; Subject-filter removal and expanded search; bounded card text; full wrapping popup titles/tags; fixed popup action placement; EN/VI labels; and synchronized requirements in specifications 026 and 027.
- **Independent Content & Student Validation:** The student supplied the group leader’s final review, selected a separate Invitations tab, decided that only Pending actionable invitations should remain visible there, required accepted invitations to move to Joined, rejected Cancel Request for invite relationships, refined the action label from Join Group to Accept Invitation, and iteratively corrected card versus popup overflow behavior. The student also required that unit tests remain untouched during these refinements and reviewed the resulting interface behavior through successive corrections.

---

### Note 2 - Adding unit and API integration tests for Create Study Group

- **Tool name, version, and platform:** OpenAI Codex, GPT-5, OpenAI Codex coding-agent workspace connected to the local project repository.
- **Access Time (Date and Hour):** July 31, 2026, 21:30–22:00 (UTC+7).
- **Prompts used:**
  - “Tham khảo unit test hiện tại và viết unit test cho use case Create Study Group. Chỉ viết unit test, không phải integration/E2E test. Test các hàm trong luồng xử lý của use case, gồm `createStudyGroupController`, `createStudyGroup`, và các hàm con mà `createStudyGroup` gọi nếu có logic cần test. Tổng số ít nhất 10 test case cho toàn bộ use case, đặt tên file test theo tên use case, tuân theo convention test hiện có, chạy test và đảm bảo tất cả đều pass. Cập nhật tài liệu mô tả nếu cần thiết trong `specs/026-study-group-feature` và `specs/027-study-group-logic-hardening`.”
  - “Không giới hạn cứng ở controller hoặc service; hãy test các đơn vị có logic quan trọng trong luồng Create Study Group.”
  - “Nhóm trưởng có nói thêm ‘Rồi viết Supertest ở folder integration nữa’. Bổ sung đi và xác nhận mọi testing ở trên chỉ liên quan đến Create Study Group.”
- **Purpose of use:** Add focused automated coverage for the Create Study Group use case, first at unit boundaries and then at the HTTP route boundary after the team leader expanded the requested scope to include Supertest integration coverage.
- **Content generated by AI:** Unit tests for request validation and normalization middleware, `createStudyGroupController`, `createStudyGroup`, metadata normalization helpers, authoritative slot and capacity checks, transactional call ordering, structured error mapping, and realtime emission behavior. AI also generated a Supertest suite for `POST /api/study-groups` covering successful creation, token and role guards, request validation, expected service errors, and unexpected server failures, plus Vitest project registration for the new files.
- **Independent Content & Student Validation:** The student defined the original unit-only boundary, clarified that important logic could be tested outside a fixed controller/service split, relayed the team leader’s later Supertest requirement, explicitly authorized the integration-test addition, and required confirmation that all newly added cases remained scoped to Create Study Group. The resulting 27 unit tests and 11 integration tests were run locally; the Create Study Group integration file passed 11/11, the Study Group project passed 48/48, and the complete backend suite passed 133/133.

---

### Note 3 - Specifying and implementing Admin System Configuration

- **Tool name, version, and platform:** OpenAI Codex, GPT-5, OpenAI Codex coding-agent workspace connected to the local project repository.
- **Access Time (Date and Hour):** August 1, 2026 (UTC+7).
- **Prompts used:**
  - “`$speckit-specify` Tôi đang muốn dựng trang System Configuration được điều hướng từ trang dashboard của chỉ riêng tài khoản admin. Để giúp admin có thể dễ dàng chỉnh sửa thông số của hệ thống. Làm việc trong `specs/029-admin-system-configuration`, tham khảo `admin-system-configuration-layout.txt`, sử dụng code trong `client/app/dashboard/admin`, và đưa `DAMAGE_COEFFICIENTS`, `FEE_ADMIN`, `FEE_ADDON`, cùng `MAX_BORROW_LIMIT` vào một file `system-configuration.json` hợp lý mà không đụng đến database.”
  - “Chạy `$speckit-plan`, `$speckit-tasks`, và `$speckit-analyze`; không cần viết unit tests cho phần này; thêm constraint không được phép Save nếu có một giá trị rỗng; làm việc trong nhánh hiện tại.”
  - “`MAX_BORROW_LIMIT` là chính giới hạn mượn đồng thời của hệ thống, không phải cận trên của con số được set.”
  - “`$speckit-implement` thực hiện toàn bộ kế hoạch và đồng bộ tài liệu đặc tả cần thiết trước khi triển khai.”
  - “Giao diện phải giống với file `.txt`; các thông số trong file tham khảo chỉ khác nội dung nên hãy giữ thiết kế cũ, thay bằng các thông số System Configuration cần quản lý.”
  - “Kiểm tra vì dường như chỉ Borrowing Policy hoạt động, còn Fee Settings và Damage Coefficients không phản ánh thay đổi trong luồng trả sách.”
  - “Nếu admin thay đổi thông số khi librarian đang return một quyển sách, hãy thông báo hệ thống vừa thay đổi, hủy phiên Return PIN cũ và điều hướng librarian về Enter Return PIN để tạo và nhập PIN mới.”
  - “Công thức penalty phải cộng dồn từng lỗi; thêm một lỗi phải tăng ít nhất bằng mức phạt độc lập của lỗi đó, không được tạo hiệu ứng giảm giá khi số lỗi tăng.”
- **Purpose of use:** Specify, plan, implement, diagnose, and refine an administrator-only file-backed configuration feature that updates borrowing and return-penalty policies without storing configuration in the database or restarting the server.
- **Content generated by AI:** Spec Kit artifacts under `specs/029-admin-system-configuration`; a validated and atomically replaced JSON configuration document; admin-only GET/PUT endpoints; runtime configuration snapshots and version conflict handling; dynamic borrowing-limit and penalty consumers; the System Configuration page and localized form components; librarian-side server-calculated penalty previews; Return PIN configuration-version binding and stale-session invalidation; cumulative damage-penalty calculation; and API integration tests.
- **Independent Content & Student Validation:** The student selected file-backed JSON instead of database persistence, prohibited feature-specific unit tests, clarified the meaning of `MAX_BORROW_LIMIT`, rejected an initial interface redesign and required fidelity to the supplied layout, manually tested the feature, discovered that the librarian preview retained hard-coded coefficients, identified stale Return PIN behavior after configuration changes, and repeatedly corrected the penalty formula. The final accepted cumulative rule treats each damage condition as a full standalone charge (`book price × coefficient + FEE_ADMIN`) and adds `FEE_ADDON` for each condition after the first. The student also separated unrelated repository-wide lint cleanup from this feature.

## V. 24127095 - Vũ Duy Nhất
### Note 1 - Find potential bugs in the system
- **Tool name, version, and platform:** Antigravity (Gemini 3.1 Pro)
- **Access Time (Date and Hour):** August 8, 2026 at 16:15
- **Prompts used:** Scan the source code in the src folder and list potential bugs may appear in the system
- **Purpose of use:** To proactively identify latent bugs in the codebase before final delivery, so that critical issues can be triaged and fixed within the PA4 sprint without waiting for manual testing to surface them.
- **Content generated by AI:** The potential bug list as the table with the bug and its location.
- **Independent Content & Student Validation:** The student reviewed the generated bug table against the actual source code, verified each reported location, removed false positives, and prioritised the confirmed issues for the team to address before submission.
### Note 2 - Drawing Deployment Diagram
- **Tool name, version, and platform:** Antigravity (Sonnet 4.6)
- **Access Time (Date and Hour):** August 5, 2026 at 15:00
- **Prompts used:** Based on the deploymentDiagramRequirent.md, draw the deployment diagram using mermaid, then create file DeploymentDiagram.md and insert into it.
- **Purpose of use:** To ensure the deployment diagram follows strictly the requirements in PA4 and is structurally consistent with the Container Diagram (C4 level 2).
- **Content generated by AI:** Mermaid diagram in the file DeploymentDiagram.md
- **Independent Content & Student Validation:** The student cross-checked every node, relationship, and label in the generated diagram against the PA4 requirements document and the existing Container Diagram, corrected any inconsistencies in naming and boundary groupings, and confirmed the final Mermaid syntax renders correctly before committing the file.